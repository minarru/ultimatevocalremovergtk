using Gtk 4.0;
using Adw 1;

template $DualInputsRow: Adw.ExpanderRow {
  title: "Input pairs";

  [suffix]
  Gtk.Button edit_button {
    label: "Pair editor…";
    valign: center;
    styles ["flat"]
  }
}
