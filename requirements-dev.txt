# Dev / CI tooling (not required at runtime).
# basedpyright is a pyright fork: same CLI, same pyrightconfig.json, plus a
# baseline mechanism for ratcheting strictness. 1.39.9 tracks pyright 1.1.411.
basedpyright==1.39.9

# Fast local linting and formatting. This is intentionally not a CI gate while
# the existing tree-wide lint and formatting backlog is being evaluated.
ruff==0.16.4

# GTK/Adw types live in requirements-stubs.txt — they must be installed with
# --no-deps, so they cannot be listed here.
