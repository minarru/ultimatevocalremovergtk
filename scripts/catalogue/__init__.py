"""One collection, reconciliation, validation, and rendering pipeline.

The generator imports the focused modules explicitly so the package does not
create import cycles while assembling the unified publication bundle.
"""
