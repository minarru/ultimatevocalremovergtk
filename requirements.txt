# UVR pinned dependencies (generated from the working .venv lock).
#
# numpy is pinned to 2.3.2 (was 2.2.6): 2.2.6 has no cp314 wheel, while 2.3.2
# does and still satisfies numba 0.65.1 (numpy<2.5,>=1.22) and torch. This is
# the single dependency change required to run the ML stack on system
# Python 3.14 alongside system PyGObject (see install_packages.sh / README).
#
# onnxruntime is the CPU build by default; install_packages.sh --cuda installs
# requirements-cuda-linux.txt (onnxruntime-gpu + NVIDIA CUDA wheels).
absl-py==2.4.0
aiohappyeyeballs==2.6.2
aiohttp==3.14.1
aiosignal==1.4.0
altgraph==0.17.5
antlr4-python3-runtime==4.9.3
attrs==26.1.0
audioread==3.1.0
# Roformer (BS-Roformer / Mel-Band Roformer) architecture deps. Vendored in
# ml/{attend,bs_roformer,mel_band_roformer}.py; einops/librosa/packaging are
# already pinned above. beartype and rotary-embedding-torch (below) are the
# base roformer deps. PoPE-pytorch + its own deps (einx, frozendict,
# torch-einops-utils) are only for BSRoformer's optional ``use_pope`` path
# (community "BS PolarFormer" checkpoints) -- see ml/bs_roformer.py.
beartype==0.22.9
certifi==2026.6.17
cffi==2.0.0
charset-normalizer==3.4.7
cython==3.2.6
decorator==5.3.1
diffq==0.2.4
einops==0.8.2
einx==0.4.3
filelock==3.29.4
flatbuffers==25.12.19
frozendict==2.4.7
frozenlist==1.8.0
fsspec==2026.6.0
future==1.0.0
idna==3.18
jinja2==3.1.6
joblib==1.5.3
julius==0.2.8
kthread==0.2.3
lazy-loader==0.5
librosa==0.11.0
lightning-utilities==0.15.3
llvmlite==0.47.0
markupsafe==3.0.3
matchering==2.0.6
ml-collections==1.1.0
ml-dtypes==0.5.4
mpmath==1.3.0
msgpack==1.2.1
multidict==6.7.1
narwhals==2.22.1
natsort==8.4.0
networkx==3.6.1
numba==0.65.1
# numpy: was 2.3.2, bumped to 2.3.5. pandas==3.0.4 requires numpy>=2.3.3, so
# 2.3.2 is unsatisfiable; 2.3.5 is the latest 2.3.x (still <2.5 for numba 0.65.1,
# satisfies pandas, has cp314 wheels, and shadows the system numpy 2.5).
numpy==2.3.5
omegaconf==2.3.1
onnx==1.22.0
onnx2pytorch==0.5.3
onnxruntime==1.26.0
# opencv-python 4.12.0.88 caps numpy<2.3.0 (all py>=3.9), which conflicts with
# the numpy==2.3.2 pin below under strict resolvers (uv) on Python 3.14. 4.13.x
# drops the upper bound (numpy>=2) and ships cp37-abi3 wheels usable on 3.14.
opencv-python==4.13.0.92
packaging==26.2
pandas==3.0.4
patsy==1.0.2
pillow==12.2.0
platformdirs==4.10.0
pooch==1.9.0
PoPE-pytorch==0.2.1
propcache==0.5.2
protobuf==7.35.1
psutil==7.2.2
pycparser==3.0
pydub==0.25.1
pyglet==2.1.15
pyperclip==1.11.0
pyrubberband==0.4.0
python-dateutil==2.9.0.post0
pytorch-lightning==2.6.5
pyyaml==6.0.3
requests==2.34.2
resampy==0.4.3
rotary-embedding-torch==0.9.1
samplerate==0.2.4
scikit-learn==1.9.0
scipy==1.16.3
screeninfo==0.8.1
setuptools==81.0.0
six==1.17.0
soundfile==0.14.0
soundstretch==1.2
soxr==1.1.0
statsmodels==0.14.6
sympy==1.14.0
threadpoolctl==3.6.0
torch==2.12.1
torch-einops-utils==0.1.15
torchmetrics==1.9.0
torchvision==0.27.1
tqdm==4.68.3
triton==3.7.1
typing-extensions==4.15.0
urllib3==2.7.0
wget==3.2
yarl==1.24.2
