name: Test

on:
  push:
    branches: [main]
  # Deliberately unfiltered. `branches:` here matches the PR's *base*, so
  # `[main]` silently skipped every stacked PR (a feature branch targeting
  # another feature branch): no checks appeared at all, and retargeting to main
  # did not help either, because that fires `edited`, which is not one of the
  # default activity types (opened/synchronize/reopened). Running on every base
  # does not double up — `push` is limited to main, so a PR branch only ever
  # fires this event.
  pull_request:
  # Lets a run be re-triggered from the Actions tab without an empty commit.
  workflow_dispatch:

jobs:
  unittest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        timeout-minutes: 4
        run: |
          # Azure is often black-holed (Ign after connect timeout). The
          # mirrorlist then fails over to https://archive.ubuntu.com, which
          # connects but stalls mid-InRelease — apt's Timeout only covers
          # connect, so the previous step sat until the 5m cap.
          # Pin HTTP Canonical. Prefer the image's pre-populated lists so a
          # healthy run never fetches InRelease. python3/pip/venv are already
          # on the image.
          printf '%s\n' \
            'APT::Acquire::Retries "1";' \
            'Acquire::http::Timeout "15";' \
            'Acquire::ForceIPv4 "true";' \
            | sudo tee /etc/apt/apt.conf.d/99ci-timeouts >/dev/null
          printf '%s\n' 'http://archive.ubuntu.com/ubuntu/' \
            | sudo tee /etc/apt/apt-mirrors.txt >/dev/null
          sudo sed -i 's|mirror+file:/etc/apt/apt-mirrors.txt|http://archive.ubuntu.com/ubuntu/|' \
            /etc/apt/sources.list.d/ubuntu.sources
          pkgs=(python3-gi gir1.2-gtk-4.0 gir1.2-adw-1 ffmpeg rubberband-cli xvfb)
          if ! sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends "${pkgs[@]}"; then
            sudo apt-get update \
              -o Dir::Etc::sourcelist="sources.list.d/ubuntu.sources" \
              -o Dir::Etc::sourceparts="-" \
              -o APT::Get::List-Cleanup="0"
            sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends "${pkgs[@]}"
          fi

      - name: Create virtual environment
        run: |
          # Match install_packages.sh: system GTK/gi via python3-gi.
          python3 -m venv --system-site-packages .venv
          .venv/bin/pip install --upgrade pip
          .venv/bin/pip install -r requirements.txt

      - name: Run unit tests
        env:
          # Software GL path avoids flaky GPU/Vulkan init on hosted runners.
          GSK_RENDERER: cairo
        run: xvfb-run -a .venv/bin/python -m unittest discover -s tests -v

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create virtual environment
        run: |
          python3 -m venv --system-site-packages .venv
          .venv/bin/pip install --upgrade pip
          .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
          # Separate, --no-deps: see requirements-stubs.txt.
          .venv/bin/pip install --no-deps -r requirements-stubs.txt

      - name: Run basedpyright
        run: .venv/bin/python -m basedpyright
